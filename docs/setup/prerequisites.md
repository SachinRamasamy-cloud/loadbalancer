# Prerequisites

Install Docker Engine, Docker Compose v2, Git, and `curl`. Node.js and Python are required for direct non-container development.

```bash
docker --version
docker compose version
git --version
curl --version
docker run --rm hello-world
```
