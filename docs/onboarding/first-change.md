# First Change

A suitable first task is adding a `backend-medium` profile.

```env
BACKEND_NAME=backend-medium
RESPONSE_DELAY_MS=250
FAILURE_RATE=0.05
```

Update Docker Compose, the host allowlist, seed configuration, tests, and documentation.
