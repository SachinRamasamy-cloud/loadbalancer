# Project Tour

```text
backend/         FastAPI load balancer
frontend/        Angular dashboard
test-backends/   Simulated backend services
docs/            Technical documentation
scripts/         Helper scripts
docker-compose.yml
```

Start with `backend/app`, `frontend/src`, `test-backends/app.py`, and `docker-compose.yml`.
