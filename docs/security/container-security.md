# Container Security

- Pin base image versions
- Run as non-root
- Use read-only filesystems where possible
- Drop unnecessary capabilities
- Do not mount the Docker socket
- Scan images
- Keep backend ports internal
- Avoid secrets in image layers
