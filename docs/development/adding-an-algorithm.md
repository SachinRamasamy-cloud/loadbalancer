# Adding a Load-Balancing Algorithm

1. Define the algorithm contract.
2. Implement backend selection.
3. Exclude unhealthy backends.
4. Add deterministic tests.
5. Add concurrency tests.
6. Expose the option through the API.
7. Add dashboard support.
8. Update documentation.

Cover empty pools, one healthy backend, zero weights, concurrent requests, and state changes during selection.
