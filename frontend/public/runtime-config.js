// Runtime configuration loaded before Angular starts.
// Local development defaults are shown below.
// For production, replace apiUrl with the public HTTPS FastAPI URL.
window.__LOADFLOW_CONFIG__ = {
  apiUrl: 'http://localhost:8080',
  apiKey: 'change-me'
};
